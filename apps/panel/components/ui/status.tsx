import { forwardRef, type HTMLAttributes, type ReactNode } from "react";
export const EmptyState = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement> & { title?: ReactNode }>(function EmptyState({ title, children, className = "", ...props }, ref) { return <div {...props} ref={ref} className={`empty${className ? ` ${className}` : ""}`} role="status">{title && <strong>{title}</strong>}{children && <div>{children}</div>}</div>; });
export const ErrorState = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement> & { title?: ReactNode }>(function ErrorState({ title = "Algo deu errado", children, className = "", ...props }, ref) { return <div {...props} ref={ref} className={`error${className ? ` ${className}` : ""}`} role="alert"><strong>{title}</strong>{children && <div>{children}</div>}</div>; });
export const LoadingState = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement> & { label?: string }>(function LoadingState({ label = "Carregando conteúdo", className = "", ...props }, ref) { return <div {...props} ref={ref} className={`loading-state${className ? ` ${className}` : ""}`} role="status" aria-live="polite" aria-busy="true"><span className="sr-only">{label}</span><div className="skeleton" aria-hidden="true" /></div>; });
export function Progress({ value, label, className = "" }: { value: number; label?: string; className?: string }) { const bounded = Math.max(0, Math.min(100, value)); return <div className={`progress ${className}`.trim()} role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={bounded} aria-label={label}><span style={{ width: `${bounded}%` }} /></div>; }
